import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    instructions: {
        textAlign: 'center',
        color: '#ffffff',
        fontFamily: 'GillSans-Light',
        marginBottom: 5,
        fontSize: 26
    },
    titles: {
        fontFamily: 'GillSans-Light',
        letterSpacing: 2,
        fontSize: 32,
        fontWeight: "normal"
    },
});

export default styles;
