import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    instructions: {
        textAlign: 'center',
        color: '#ffffff',
        fontFamily: 'GillSans-Light',
        marginBottom: 5,
    },
    titles: {
        fontFamily: 'GillSans-Light',
        letterSpacing: 2
    },
});

export default styles;
